[package]
name = "conformance"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
host-adapter = { path = "../host-adapter" }
orchestrator = { path = "../orchestrator" }

[dev-dependencies]
mock-adapter = { path = "../mock-adapter" }
